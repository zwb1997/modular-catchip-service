<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zzz.service.module.mapper.ActorMapper">
    <resultMap id="actorMap" type="com.zzz.entitymodel.servicebase.DO.ActorModel">
        <id column="actor_id" property="actorId"/>
        <result column="first_name" property="firstName"/>
        <result column="last_name" property="lastName"/>
        <result column="last_update" property="lastUpdate"/>
    </resultMap>

    <select id="queryActors" resultMap="actorMap">
        SELECT
        actor_id
        ,first_name
        ,last_name
        ,last_update
        FROM actor
        <where>
          <if test="params != null and params.lastName != null">
              last_name = #{params.lastName}
          </if>
        </where>
    </select>
</mapper>